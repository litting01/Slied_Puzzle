난이도 (3x3,5x5,7x7)
마우스로 클릭해서 움직일 조각 선택후 wasd로 움직임

ctrl z,y;
객체가 행동 할때마다 로그를 저장
로그 내용(예시)
*턴 로그 //턴별로 로그생성 (배열로 하면 될듯)
객체.move(1,0) //객체가 왼쪽으로 이동
객체.finish(객체) //어떤 객체가 지정된 객체에 도달함

키를 누를때


ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ
게임 정보 클래스
	스테이지 매니저
	이미지 리스트
	턴,최대 턴
	

노드 클래스
	pictureBox
public:
	//자신의 렉트에 맞춰서 자름
	void CutBitmap(Bitmap)
	//배열 인덱스를 x,y로 받아 나눈 사이즈에 맞게 렉트 설정
	void SetRect(int x,int y,int size)
	
스테이지 매니저 클래스
	스테이지
	난이도
	선택한 노드
스테이지 클래스
private:
	SizeH
	SizeW
	노드[,]
public:
	//난이도에 맞게 가로세로 길이 정하고 노드배열 생성
	void CreateStage(난이도)
		//빈칸 하나 만들때 여기서 만들까?

	//노드의 픽쳐박스를 컨트롤에 추가함
	void PrintStage()
	
	//노드를 빈곳과 서로 바꾸는것
	
	//노드 위치를 섞어서 퍼즐만드는 것
		//움직일수 있는노드들중 랜덤으로 움직이기

	//퍼즐을 풀수 있는지 확인 하는 것?



로그 클래스
	메세지(스택)